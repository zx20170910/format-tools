# 格式化工具箱 Chrome 扩展

## 本地安装

1. 打开 Chrome，访问 `chrome://extensions/`。
2. 开启右上角的“开发者模式”。
3. 点击“加载已解压的扩展程序”。
4. 选择目录：`D:\Tools-extension`。

扩展不依赖外部 CDN，工具数据只在本地页面中处理。

点击浏览器工具栏中的扩展图标后，会先展示功能项列表；选择任一功能后，工具会在新的浏览器页签中打开。工具页内仍可通过顶部标签切换其他功能。

在线地址：<https://zx20170910.github.io/format-tools/>
